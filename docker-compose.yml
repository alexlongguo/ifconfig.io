version: "3.4"

services:
  ifconfig:
    image: ip123.top:latest
    #image: georgyo/ifconfig.io:latest
    build:
      context: ./
      target: production
    ports:
      - ${PORT:-80}:80
    environment:
      TLS: ${TLS:-0}
      HOSTNAME: ${HOSTNAME}
